<%= form_with(model: sock, local: true) do |form| %>
  <% if sock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sock.errors.count, "error") %> prohibited this sock from being saved:</h2>

      <ul>
      <% sock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :material %>
    <%= form.text_field :material, id: :sock_material %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :sock_color %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :sock_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
